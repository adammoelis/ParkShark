$('.all-spots').html('<%= j render partial: "listings/listing_stand_alone", locals: {listings: @listings} %>')

// $('#map').html("")
handler.removeMarkers(storedMarkers)
markers = <%= raw @hash.to_json %>
storedMarkers = markers.map(function(m) {
  marker = handler.addMarker(m);
  marker.serviceObject.set('id', m.id); // You can add other attributes using set
  return marker;
});
handler.bounds.extendWith(storedMarkers);
handler.fitMapToBounds();
