<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="<%= root_path %>">ParkShark</a>
    </div>
    <ul class="nav navbar-nav navbar-right">
      <li><%= link_to 'All Spots', spots_path %></li>
      <li><%= link_to 'View Users', users_path %></li>
      <% if user_signed_in? && current_user %>
        <li><%= link_to 'Edit Profile', edit_user_path(current_user) %></li>
        <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
        <li><p class="navbar-text">Signed in as <%= link_to current_user.email, user_path(current_user), class: 'navbar-link' %></p></li>
        <li class="round-image-50"><%= image_tag(current_user.avatar.url(:thumb)) %></li>
      <% else %>
        <li><%= link_to "Log in", new_session_path(resource_name) %></li>
        <li><%= link_to "Sign up", new_registration_path(resource_name) %></li>
      <% end %>
    </ul>
  </div>
</nav>
